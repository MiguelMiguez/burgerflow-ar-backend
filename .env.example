# Server Configuration
PORT=3000

# Firebase Configuration
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_CLIENT_EMAIL=service-account@your-project.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nreplace-with-your-key\n-----END PRIVATE KEY-----\n"
GOOGLE_APPLICATION_CREDENTIALS=path-to-google-credentials.json

# OpenAI & Dialogflow (optional)
OPENAI_API_KEY="your-openai-api-key"
DIALOGFLOW_PROJECT_ID="example-project-id"
DIALOGFLOW_LANGUAGE_CODE="example-language-code"

# API Keys para autenticación
ADMIN_API_KEY=admin-key
USER_API_KEY=user-key

# Meta WhatsApp Business Cloud API
# Token para verificar el webhook (puedes usar cualquier string, ej: "mi_token_secreto_123")
META_VERIFY_TOKEN=your_webhook_verify_token_here

# Secret de la app de Meta (opcional, para verificar firma de webhooks)
META_APP_SECRET=your_meta_app_secret_here

# Versión de la API de Meta Graph (por defecto v21.0)
META_API_VERSION=v21.0

# NOTAS IMPORTANTES:
# 1. META_VERIFY_TOKEN: Este token lo defines tú y lo usas al configurar el webhook en Meta
# 2. Los tokens de acceso (metaAccessToken) y Phone Number IDs (metaPhoneNumberId) 
#    se gestionan por TENANT en la base de datos, NO aquí en el .env
# 3. META_APP_SECRET es opcional pero recomendado para validar la firma de los webhooks
# 4. Las variables WHATSAPP_* antiguas ya NO son necesarias (whatsapp-web.js removido)

